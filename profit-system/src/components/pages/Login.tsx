import { Box, Flex, Heading } from "@chakra-ui/react";
import { FC, memo } from "react";

export const Login: FC = memo(() => {
    return (
        <Flex>
            <Box>
                <Heading></Heading>
            </Box>
        </Flex>
    );
});